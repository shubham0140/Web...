// This is a comment

console.log("Hell World !!");

// String template literals
let firstName = 'Muskan';
let lastName = 'Raj';

//let fullName = "Miss." + lastName + "," + firstName;
//console.log(fullName);

// Better Way-- using String template literals
let fullName = `Miss.${lastName},${firstName}`;

console.log(fullName);

